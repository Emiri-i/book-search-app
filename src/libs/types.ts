export interface bookType {
  id: string;
  title: string;
  image: any;
  description: string;
  readDate: string;
  memo: string;
}

export interface navItemType {
  name: string;
  icon: string;
  url: string;
}

export interface searchResultsType {
  title: string;
  img: any;
  description: string;
  id: string;
  index: number;
}
