export interface bookType {
  id: number;
  title: string;
  image: any;
  description: string;
  readDate: string;
  memo: string;
}

export interface navItemType {
  name: string;
  icon: string;
  url: string;
}
